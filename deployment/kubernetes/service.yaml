---
# Service for druva server
apiVersion: v1
kind: Service
metadata:
  name: druva-server
  namespace: ${NAMESPACE}
  labels:
    app: druva
    component: server
  annotations:
    # Optional: Add service annotations for cloud providers
    # cloud.google.com/load-balancer-type: "Internal"
spec:
  type: ClusterIP  # Use LoadBalancer or NodePort for external access
  sessionAffinity: None  # Could use ClientIP for sticky sessions
  ports:
  - name: druva
    port: 2972
    targetPort: 2972
    protocol: TCP
    # nodePort: 32972  # Required if type=NodePort (30000-32767 range)
  selector:
    app: druva
    component: server

---
# Optional: Headless service for StatefulSet
# Uncomment if using StatefulSet instead of Deployment
# apiVersion: v1
# kind: Service
# metadata:
#   name: druva-server-headless
#   namespace: ${NAMESPACE}
#   labels:
#     app: druva
#     component: server
# spec:
#   type: ClusterIP
#   clusterIP: None
#   ports:
#   - name: druva
#     port: 2972
#     targetPort: 2972
#     protocol: TCP
#   selector:
#     app: druva
#     component: server

---
# Optional: ServiceMonitor for Prometheus Operator
# Uncomment if you have Prometheus Operator installed
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: druva-server
#   namespace: ${NAMESPACE}
#   labels:
#     app: druva
# spec:
#   selector:
#     matchLabels:
#       app: druva
#       component: server
#   endpoints:
#   - port: druva
#     interval: 30s
#     scrapeTimeout: 10s
#     path: /metrics
#     scheme: http

---
# Optional: Ingress for external access (if using LoadBalancer)
# Uncomment and configure based on your ingress controller
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: druva-server
#   namespace: ${NAMESPACE}
#   labels:
#     app: druva
#   annotations:
#     # Example annotations for NGINX Ingress Controller
#     nginx.ingress.kubernetes.io/backend-protocol: "TCP"
#     nginx.ingress.kubernetes.io/ssl-passthrough: "true"
# spec:
#   ingressClassName: nginx
#   rules:
#   - host: druva.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: druva-server
#             port:
#               number: 2972
