# Druva MCP Server Configuration

storage:
  path: ~/.local/share/druva/druva.druva
  read_only: false
  backend: file

adapters:
  enable_versioning: true
  enable_health_checks: true
  max_versions_per_adapter: 10
  auto_push_on_startup: true
