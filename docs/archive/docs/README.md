# Archived Docs

This folder contains historical supporting documents that have been superseded.

- Legacy naming and import paths may appear here.
- Commands may not reflect the current `dhara` CLI surface.
- Prefer the active `/docs` tree for current usage.
