# Archive

These documents are retained for historical context only.

- They may reference legacy `Durus` or `Druva` names.
- They may document superseded commands, imports, or implementation plans.
- They should not be treated as the current Dhara runtime or operator guidance.

For current behavior, prefer the active documentation in `/docs`, the root [README.md](/Users/les/Projects/dhara/README.md), and the canonical runtime settings/model surfaces in `dhara.core.config` and `dhara.mcp.server_core`.
